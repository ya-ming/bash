#!/bin/bash
a=1
echo my PID is $$
b=2
echo a is $a b is $b c is $c
if
true
tehn
   echo true is successful
else
   echo true is not successful
fi


#bash debug1.sh     #run the script without debugging
#bash -x debug1.sh  #echo each commands
#bash -n debug1.sh  #echo syntax erros